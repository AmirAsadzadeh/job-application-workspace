# Runtime Company Logos

This directory contains company logos uploaded while creating positions.

- Stored filenames are generated by the service and never use a client path.
- Supported formats are PNG, JPEG, and SVG, with a 2 MB decoded size limit.
- Position records reference files through `/company-logos/<generated-name>`.
- Do not place credentials, private documents, or unrelated assets here.
