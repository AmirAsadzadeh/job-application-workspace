# Company logo assets

Position records may reference only a direct child of this directory using
`/company-logos/<filename>`. The UI renders a compact company-initial fallback when the
path is null or an image cannot be loaded. Files outside this public asset directory are
never served by the positions API.
